version: '3'
services:
  mysql:
    image: mysql:latest
    ports:
      - 8080:3306
    volumes:
      - ./Database:/docker-entrypoint-initdb.d
    environment:
      MYSQL_ROOT_PASSWORD: Molly
      MYSQL_DATABASE: US_GEO